# Gemini (chat Caju) — opcional; sem chave o chat informa que não está configurado
GEMINI_API_KEY=

# Supabase (avaliações persistentes) — opcional; sem isso usa localStorage
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
